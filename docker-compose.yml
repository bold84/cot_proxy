version: '3.8'

services:
  cot-proxy:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ENV_TARGET_BASE_URL=http://your-model-server:8080/
      - DEBUG=false
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
